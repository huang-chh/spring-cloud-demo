server:
  port: 8002
eureka:
  client:
    service-url:
      defaultZone: http://eureka8761.com:8761/eureka/,http://eureka8762.com:8762/eureka/,http://eureka8763.com:8763/eureka/
  instance:
    instance-id: test-cloud-provider8002
    prefer-ip-address: true
spring:
  application:
    name: test-cloud-provider
#需要引入actuator
info:
  app.name: testCloudProvider
  company.name: www.annie.com
  build.artifactId: ${project.artifactId}
  build.version: ${project.version}
